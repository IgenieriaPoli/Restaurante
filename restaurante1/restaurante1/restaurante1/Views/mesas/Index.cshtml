@model IEnumerable<restaurante1.Models.mesas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.comensales)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado_mesa.estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.comensales)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado_mesa.estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.numero_mesa }) |
            @Html.ActionLink("Details", "Details", new { id=item.numero_mesa }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.numero_mesa })
        </td>
    </tr>
}

</table>
